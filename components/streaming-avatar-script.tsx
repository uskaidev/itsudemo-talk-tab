"use client";

// Note: We're not using the Script component to load the SDK from a CDN URL anymore
// Instead, we're relying on the npm package @heygen/streaming-avatar
// which is imported directly in the components that need it

export default function StreamingAvatarScript() {
  // This component is now a placeholder and doesn't do anything
  // The SDK is imported directly in interactive-avatar.tsx
  return null;
}
